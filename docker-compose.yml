version: "3.9"

services:
  trademarkia-api:
    build: .
    container_name: trademarkia-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./:/app
    command: uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload
